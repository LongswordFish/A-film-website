<section>
    <div style="height:400px;">

    </div>
        <!-- Modal credit to bootstrap-->
        <div class="modal fade" id="registerModel" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
            aria-labelledby="staticBackdropLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="staticBackdropLabel">Register</h5>
                        <button id="closeBtn" type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>                     
                    </div>
                    <div class="modal-body">
                        <form id="register" action="/user/register" method="POST">
                            Username<input id="username_res" name="name" type="text" tabindex="1" maxlength="25" value={{name}}> 
                            {{#if error_0}}
                            <p style="color:red; font-size:0.8em;">{{error_0}}</p>
                            {{/if}}       
                            Password<input id="password_res" name="password" type="password"  tabindex="2" placeholder="Aa111111" value={{password}}>
                            {{#if error_1}}
                            <p style="color:red; font-size:0.8em;">{{error_1}}</p>  
                            {{/if}}
                            {{#if error_2}}
                            <p style="color:red; font-size:0.8em;">{{error_2}}</p>  
                            {{/if}}        
                            Email Address<input id="email_res" name="email" autocomplete="email" tabindex="3" placeholder="1@1.ca" value={{email}} >
                            {{#if error_3}}
                            <p style="color:red; font-size:0.8em;">{{error_3}}</p>
                            {{/if}}
                            {{#if error_5}}
                            <p style="color:red; font-size:0.8em;">{{error_5}}</p>
                            {{/if}}
                            Phone<input id="phone_res" name="phone"  tabindex="4" placeholder="1111111111" value={{phone}} >
                            {{#if error_4}}
                            <p style="color:red; font-size:0.8em;">{{error_4}}</p>
                            {{/if}} 
                            <input type="hidden" name="form_name" value ="register"/>  
                            <div class="modal-footer">
                                <button type="reset" id="clearBtn" class="btn btn-secondary">Clear</button>
                                <input type="submit" class="btn btn-primary"/>
                            </div>
                            <div style="display: flex; justify-content: space-between;padding:0.75rem">
                            <span>Already have an account?</span><a href="/signIn"> <input class="btn btn-success" style="margin-left:auto" value="Sign In"/> </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <!-- Modal credit to bootstrap-->
</section>
